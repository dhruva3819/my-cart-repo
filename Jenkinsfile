pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo "***** coming from GitHub repo *****"
                echo "***** Building from scm *****"
            }
        }
    }
}
