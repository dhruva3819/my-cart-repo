pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Welcome to jenkins pipeline"
                error "This will giv egive some error"
            }
        }
    }
}
